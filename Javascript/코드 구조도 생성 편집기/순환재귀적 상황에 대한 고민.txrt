    /*
    순환재귀적 상황 해결 방안

    1. 한 번 표시한 함수는 다시 표시 안 함
    2. 순환재귀적 상황을 탐지하는 트리구조와 검색 알고리즘을 만듬

    2번의 방법은 순환재귀적 상황 발생 시 출력을 멈추므로 일관성이 떨어져서
    1번의 방법으로 가는 것이 타당한 것 같다.


    1번 구현방법:
    함수를 표시할 때마다 표시한 함수를 기록한다.
    표시한 함수를 배열에서 제거하는 방식으로도 구현 가능하지만,
    그 방법이면 함수 순환 재귀 호출을 표현할 방법이 전혀 없다.

    따라서 표시한 함수는 일단 기록하고,
    다음에 앞서 표시된 함수를 호출할 경우
    연결선으로 잇는 방식으로 구현한다.

    그러므로 이거 구현하기 전에 연결선부터 구현한다.

    */